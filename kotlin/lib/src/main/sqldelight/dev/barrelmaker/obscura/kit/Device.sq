CREATE TABLE IF NOT EXISTS Device (
    device_id TEXT NOT NULL PRIMARY KEY,
    device_name TEXT NOT NULL,
    is_own INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE IF NOT EXISTS DeviceIdentity (
    id INTEGER NOT NULL PRIMARY KEY DEFAULT 1,
    device_id TEXT NOT NULL
);

selectOwnDevices:
SELECT * FROM Device WHERE is_own = 1;

insertDevice:
INSERT OR REPLACE INTO Device(device_id, device_name, is_own)
VALUES (?, ?, 1);

deleteDevice:
DELETE FROM Device WHERE device_id = ?;

selectIdentity:
SELECT * FROM DeviceIdentity WHERE id = 1;

insertIdentity:
INSERT OR REPLACE INTO DeviceIdentity(id, device_id)
VALUES (1, ?);

deleteIdentity:
DELETE FROM DeviceIdentity WHERE id = 1;

deleteAllDevices:
DELETE FROM Device;
